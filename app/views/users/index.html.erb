<h1>All Users</h1>
  <ul>
  
    <% @users.each do |user| %>
      <li><%= link_to user.name, user %>
      <%= link_to 'Delete',user, method: :delete,
                                 data: {confirm: "You sure?"} %></li>
    <% end %>
  </ul>
